let numeroIngresado= Number( prompt ('Ingresá un número para multiplicarlo') );

 for (let i = 1; i <= 10; i++) {
    let resultado = numeroIngresado * i;
   alert(numeroIngresado+ ' x '+i+ ' = '+resultado);
}

